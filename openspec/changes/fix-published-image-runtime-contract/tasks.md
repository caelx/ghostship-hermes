## 1. Publication Path

- [ ] 1.1 Update the publish workflow so immutable and mutable `ghostship-hermes` tags are published from the explicit `ghostship-hermes-image` artifact.
- [ ] 1.2 Re-key final-image reuse around the explicit publishable artifact instead of the base-plus-overlay derivations.

## 2. Documentation And Verification

- [ ] 2.1 Update the repo docs and changelog to describe the corrected GHCR publication path and runtime-contract guarantee.
- [ ] 2.2 Validate the OpenSpec change and run targeted repo checks for the updated publication workflow.
