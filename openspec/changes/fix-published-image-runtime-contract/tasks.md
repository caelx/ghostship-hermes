## 1. Publication Path

- [x] 1.1 Update the publish workflow so immutable and mutable `ghostship-hermes` tags are published from the explicit `ghostship-hermes-image` artifact.
- [x] 1.2 Re-key final-image reuse around the explicit publishable artifact and export helper instead of the base-plus-overlay derivations.

## 2. Documentation And Verification

- [x] 2.1 Update the repo docs and changelog to describe the corrected GHCR publication path and runtime-contract guarantee.
- [x] 2.2 Validate the OpenSpec change and run targeted repo checks for the updated publication workflow.
