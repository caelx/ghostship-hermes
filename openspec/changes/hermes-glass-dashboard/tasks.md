## 1. Visual Foundation (Atmosphere & Surfaces)

- [x] 1.1 Implement deep-dark background with "Nebula" radial-gradient blobs in `styles.css`.
- [x] 1.2 Define `.glass-pane` and `.glass-button` utility classes with `backdrop-filter: blur(24px)`.
- [x] 1.3 Update layout to "floating panels" (margins and rounded corners for sidebar/stage).
- [x] 1.4 Integrate "Inter" variable font and update global typography rules.

## 2. Component Refinement

- [x] 2.1 Update sidebar buttons (New Session) with glass cards and modern icons.
- [x] 2.2 Refactor session tabs into floating glass cards with "active" glow states.
- [x] 2.3 Redesign the empty state (formerly "READY") with minimalist logo and typography.
- [x] 2.4 Modernize the terminal header and close button with refined glass styles.

## 3. Interaction & Copy

- [x] 3.1 Update all military-themed labels (MMX) to modern, professional alternatives in `index.html`.
- [x] 3.2 Refine smooth transitions for tab switching and session loading.
- [x] 3.3 Ensure the loading state (`INITIALIZING_BATTLE_STATION`) is replaced with a sleek "Connecting..." glass overlay.

## 4. Integration & Validation

- [x] 4.1 Build the `hermes-dashboard` package to bundle the new assets.
- [x] 4.2 Start the dashboard within the development environment/container.
- [x] 4.3 Use `agent-browser` to visually inspect the dashboard and capture verification screenshots.
