# ghostship-plex

CLI utility for Plex API.

## Environment Variables

- `PLEX_URL`: The base URL of your Plex instance (e.g., `http://localhost:32400`).
- `PLEX_TOKEN`: Your Plex authentication token.